基于大数据的圈人营销习题

使用技术 hive + hdfs + mapreduce + spark + flink + kafka + hbase  + postgresql 构建 大数据圈人营销平台，包含实时链路和离线链路。

整体划分，1. 数据资产管理平台，使用 hive来做数据仓库，进行数据的元数据管理，包含业务主键，表中的标签字段，标签类型等信息，定时进行标签数据等质检表中的数据都是离线数据，以dt为时间分区，对外提供写入的数据接口

2. 圈人平台，对标签中的数据设置筛选条件并进行组合，然后可以设置定时更新的条件进行定时调度，人群导出的逻辑是基于人群规则（标签+条件）进行驱动spark基于hive数据仓库里的数据进行计算，并且把能导出到不同的渠道，比如以文件形式导出，或者以hive表形式导出，或者以uid为rowkey和人群code存储到hbase，然后对外提供基于uid和crowd的匹配能力
3. 活动平台 ，选择人群和时间，活动列表在活动开始之后，展示出符合对应条件 的数据
4. 人群匹配平台基于hbase中的数据和人群的元信息，进行数据实时查询


先一个项目直接写所有的代码，后面进行规划

1. 读取hive项目中的表数据，hive中创建表和数据，API可以写入数据到对应的表中
2. 创建人群，规则保存
3. 人群根据规则转成 spark任务
